# GitHub 新手教程 - 创建仓库并推送代码 📚

这是一份专为新手准备的 GitHub 使用指南，手把手教你如何创建仓库并推送代码。

## 📋 前置准备

### 1. 注册 GitHub 账号

如果你还没有 GitHub 账号：

1. 访问 [https://github.com](https://github.com)
2. 点击右上角 "Sign up"（注册）
3. 按照提示填写：
   - 邮箱地址
   - 密码
   - 用户名
4. 完成邮箱验证

### 2. 安装 Git

#### macOS 系统（你的系统）：

**方法一：检查是否已安装**
```bash
git --version
```
如果显示版本号（如 `git version 2.x.x`），说明已经安装了，可以跳过安装步骤。

**方法二：如果没有安装**
```bash
# 使用 Homebrew 安装（推荐）
brew install git

# 或者下载安装包
# 访问：https://git-scm.com/download/mac
```

#### Windows 系统：
下载安装包：https://git-scm.com/download/win

---

## 🌟 第一步：在 GitHub 上创建新仓库

### 1. 登录 GitHub

访问 [https://github.com](https://github.com) 并登录你的账号

### 2. 创建新仓库

**方式一：通过首页创建**
1. 登录后，在页面右上角找到 "+" 号
2. 点击 "+" → 选择 "New repository"（新建仓库）

**方式二：通过仓库页面创建**
1. 点击右上角你的头像
2. 选择 "Your repositories"（你的仓库）
3. 点击绿色按钮 "New"（新建）

### 3. 填写仓库信息

在创建仓库页面，填写以下信息：

#### Repository name（仓库名称）⚠️ 必填
```
点餐小程序V2
```
或者使用英文名（推荐）：
```
ordering-miniapp-v2
```

#### Description（描述）（可选）
```
基于 Vue 3 + Vite + 飞书多维表格的点餐小程序
```

#### Public / Private（公开/私有）
- **Public（公开）**：任何人都可以看到你的代码（推荐）
- **Private（私有）**：只有你和你授权的人能看到

#### 初始化选项 ⚠️ 重要
**不要勾选以下选项**（因为你的本地项目已经有代码了）：
- ❌ **不要勾选** "Add a README file"
- ❌ **不要选择** .gitignore 模板
- ❌ **不要选择** License

### 4. 创建仓库

点击绿色按钮 "Create repository"（创建仓库）

---

## 🚀 第二步：配置 Git（首次使用需要）

如果这是你第一次使用 Git，需要配置你的身份信息：

```bash
# 配置用户名（替换为你的 GitHub 用户名）
git config --global user.name "你的GitHub用户名"

# 配置邮箱（替换为你注册 GitHub 的邮箱）
git config --global user.email "你的邮箱@example.com"

# 查看配置是否成功
git config --list
```

**示例：**
```bash
git config --global user.name "zhangsan"
git config --global user.email "zhangsan@gmail.com"
```

---

## 📤 第三步：推送代码到 GitHub

### 1. 打开终端（Terminal）

在你的项目目录下打开终端：

**方法一：从 Finder 打开**
- 打开 Finder
- 找到项目文件夹：`/Users/anker/Documents/speckit/点餐小程序V2`
- 右键点击文件夹
- 选择 "服务" → "新建位于文件夹位置的终端窗口"

**方法二：使用 cd 命令**
```bash
cd /Users/anker/Documents/speckit/点餐小程序V2
```

### 2. 检查 Git 状态

```bash
# 查看当前状态
git status
```

你应该看到类似这样的输出：
```
On branch feature/ordering-system
...
```

### 3. 添加所有文件到 Git

```bash
# 添加所有文件（包括新创建的配置文件）
git add .

# 查看添加了哪些文件
git status
```

### 4. 提交更改

```bash
# 提交更改，并添加提交说明
git commit -m "添加 Vercel 部署配置和 GitHub 教程"
```

### 5. 关联远程仓库 ⚠️ 重要

创建仓库后，GitHub 会显示一个页面，上面有仓库的 URL。

**复制仓库 URL：**
- 找到类似这样的 URL：`https://github.com/你的用户名/仓库名.git`
- 例如：`https://github.com/zhangsan/ordering-miniapp-v2.git`

**关联远程仓库：**
```bash
# 方式一：HTTPS（推荐新手使用）
git remote add origin https://github.com/你的用户名/仓库名.git

# 示例
git remote add origin https://github.com/zhangsan/ordering-miniapp-v2.git
```

**验证是否关联成功：**
```bash
git remote -v
```

应该显示：
```
origin  https://github.com/你的用户名/仓库名.git (fetch)
origin  https://github.com/你的用户名/仓库名.git (push)
```

### 6. 推送代码到 GitHub

```bash
# 推送到远程仓库的 main 分支
git push -u origin feature/ordering-system
```

**第一次推送时**，会要求你输入 GitHub 的用户名和密码：
- **用户名**：你的 GitHub 用户名
- **密码**：⚠️ 注意，现在需要使用 **Personal Access Token**（个人访问令牌），而不是账号密码

---

## 🔑 第四步：创建 GitHub Personal Access Token（重要）

GitHub 现在要求使用 Token 而不是密码来推送代码。

### 1. 创建 Token

1. 登录 GitHub
2. 点击右上角头像 → "Settings"（设置）
3. 左侧菜单最下方 → "Developer settings"（开发者设置）
4. 左侧菜单 → "Personal access tokens" → "Tokens (classic)"
5. 点击 "Generate new token" → "Generate new token (classic)"

### 2. 配置 Token

填写以下信息：

**Note（备注）：**
```
点餐小程序部署用 Token
```

**Expiration（过期时间）：**
- 选择 "No expiration"（永不过期）或根据需要选择时间

**Select scopes（选择权限）：**
勾选以下权限：
- ✅ **repo**（完整的仓库控制权限）
  - 这会自动勾选所有子选项

### 3. 生成并保存 Token

1. 点击页面底部绿色按钮 "Generate token"
2. ⚠️ **重要**：复制生成的 Token（类似：`ghp_xxxxxxxxxxxxxxxxxxxx`）
3. **立即保存到安全的地方**（如密码管理器），因为刷新页面后就看不到了

### 4. 使用 Token 推送

当执行 `git push` 时：
- **Username（用户名）**：输入你的 GitHub 用户名
- **Password（密码）**：粘贴刚才复制的 Token

---

## 🎉 验证推送是否成功

### 1. 查看终端输出

推送成功会显示类似信息：
```
Enumerating objects: 50, done.
Counting objects: 100% (50/50), done.
...
To https://github.com/你的用户名/仓库名.git
 * [new branch]      feature/ordering-system -> feature/ordering-system
```

### 2. 访问 GitHub 仓库

在浏览器中打开你的仓库地址：
```
https://github.com/你的用户名/仓库名
```

你应该能看到：
- ✅ 所有的代码文件
- ✅ README.md 文件
- ✅ 提交历史

---

## 🔄 后续更新代码

以后修改代码后，只需要三步：

```bash
# 1. 添加更改
git add .

# 2. 提交更改
git commit -m "描述你做了什么修改"

# 3. 推送到 GitHub
git push
```

---

## 🛠️ 常见问题解决

### Q1: git push 时提示 "remote: Support for password authentication was removed"

**原因：** GitHub 不再支持密码认证

**解决方案：** 使用 Personal Access Token（参考上面的 Token 创建步骤）

---

### Q2: 提示 "fatal: remote origin already exists"

**原因：** 已经关联过远程仓库

**解决方案：**
```bash
# 查看当前关联的仓库
git remote -v

# 如果 URL 不对，删除后重新添加
git remote remove origin
git remote add origin https://github.com/你的用户名/仓库名.git
```

---

### Q3: 推送时提示 "failed to push some refs"

**原因：** 远程仓库有本地没有的内容（如 README.md）

**解决方案：**
```bash
# 先拉取远程代码
git pull origin feature/ordering-system --allow-unrelated-histories

# 然后再推送
git push -u origin feature/ordering-system
```

---

### Q4: 提示 "Please tell me who you are"

**原因：** 没有配置 Git 用户信息

**解决方案：**
```bash
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"
```

---

## 📝 完整操作流程总结

```bash
# 1. 配置 Git（首次使用）
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"

# 2. 进入项目目录
cd /Users/anker/Documents/speckit/点餐小程序V2

# 3. 添加文件
git add .

# 4. 提交更改
git commit -m "添加 Vercel 部署配置"

# 5. 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/仓库名.git

# 6. 推送代码
git push -u origin feature/ordering-system

# 输入用户名和 Token
```

---

## ✅ 检查清单

推送前确认：
- [ ] 已在 GitHub 上创建新仓库
- [ ] 已配置 Git 用户信息（首次使用）
- [ ] 已创建 Personal Access Token
- [ ] 已保存 Token（因为只显示一次）
- [ ] 已关联远程仓库
- [ ] 已成功推送代码

---

## 🎯 下一步

代码推送成功后，你就可以：
1. ✅ 在 Vercel 中导入这个 GitHub 仓库
2. ✅ 部署你的点餐小程序到公网
3. ✅ 分享给其他人使用

需要帮助吗？随时问我！ 🚀

---

## 📚 推荐学习资源

- [GitHub 官方文档（中文）](https://docs.github.com/zh)
- [Git 简明指南](https://www.runoob.com/manual/git-guide/)
- [Pro Git 中文版](https://git-scm.com/book/zh/v2)

祝学习顺利！ 💪
